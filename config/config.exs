import Config

# This is not evaluated when this library is used as a dependency,
# so it can be overridden in your own app config.
# When developing this library you can install dart_sass with:
# mix sass.install

config :dart_sass, version: "1.77.8"
